package streamap;
import java.util.Arrays;
import java.util.List;
import java.util.stream.Stream;

public class streamEx {
	//public static void main(String args[]) {
		//List<Integer> list = Arrays.asList(10, 12, 20, 25, 30, 11, 17, 16);

        //S//ystem.out.println("Even numbers:");

        //list.stream().
        //filter(n -> n % 2 == 0)
          // .forEach(n -> System.out.print(n + " "));
	//}
//}
class Student {
    String name;
    int marks;

    // Constructor
    Student(String name, int marks) {
        this.name = name;
        this.marks = marks;
    }
}



//        List<Student> students = Arrays.asList(
//            new Student("Amit", 75),
//            new Student("Ravi", 55),
//            new Student("Anil", 65),
//            new Student("Vikas", 40)
//        );

//        System.out.println("Students with marks > 60:");
//
//        students.stream()
//        .filter(s -> s.marks > 60)
//        .forEach(s -> System.out.println(s.name + " - " + s.marks));
//}
//}


//        List<String> list = Arrays.asList("Amit", "ravi", "Anil", "Vikas", "Ashok", "Rahul");
//
  //      System.out.println("Strings starting with A:");

    //    list.stream()
      //      .filter(s -> s.startsWith("A"))
        //    .forEach(s -> System.out.print(s + " "));
    //}
//}
    //	List<Integer> list = Arrays.asList(10, 12, 20, 25, 30, 11, 17, 16);
//
  //      System.out.println("Numbers between 50 and 60:");
//
  //      list.stream()
    //        .filter(n -> n >= 50 && n <= 60)
      //      .forEach(n -> System.out.print(n + " "));
    //}
//}import java.util.*;
    	

    	      //  List<String> list = Arrays.asList("java", "python", "c++");

    	     //   System.out.println("Strings with length greater than 4:");
//
    	       // list.stream()
    	           // .filter(s -> s.length() > 4)
    	         //   .forEach(s -> System.out.print(s + " "));
    	   // }
    //	}

       // List<Integer> list = Arrays.asList(10, null, 20, null, 30, 40);

       // System.out.println("List after removing null values:");

      //  list.stream()
         //   .filter(list::nonNull)
          //  .forEach(n -> System.out.print(n + " "));
   // } 
//}
    	// List<String> list = Arrays.asList("orange", "apple", "onion", "banana", "olive");

         //System.out.println("Strings containing 'o':");

         //list.stream()
           //  .filter(s -> s.contains("o"))
             //.forEach(s -> System.out.print(s + " "));
     //}
 //}
    	